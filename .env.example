# Groq API — get free key at console.groq.com
GROQ_API_KEY=
GROQ_MODEL=llama-3.1-8b-instant

# Qdrant Cloud — free tier at cloud.qdrant.io
# On TLS-inspecting networks, add your proxy's CA to backend/certs/
QDRANT_URL=
QDRANT_API_KEY=
QDRANT_COLLECTION=mennbridge

# PostgreSQL connection string
# Docker Compose: postgresql://postgres:postgres@postgres:5432/mennbridge
DATABASE_URL=postgresql://postgres:postgres@postgres:5432/mennbridge

# Project identifier — used to scope all memories
MENNBRIDGE_PROJECT=mennbridge-v1

# Server port (default 8001)
PORT=8001
